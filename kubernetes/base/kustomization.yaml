apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - loan-product-deployment.yaml
  - loan-product-service.yaml
  - eligibility-deployment.yaml
  - eligibility-service.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: beacon
    includeSelectors: true
